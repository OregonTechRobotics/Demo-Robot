name: Makefile CI on Pull Request

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: Build Dependecies
        uses: purduesigbots/pros-cli/checkout@v1
        run: pip install -e pros-cli
      
      - name: Build Project
        run: make quick
